apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "group5--nginx-hc.fullname" . }}-test-connection"
  labels:
    {{- include "group5--nginx-hc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "group5--nginx-hc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
