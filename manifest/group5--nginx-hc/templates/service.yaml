apiVersion: v1
kind: Service
metadata:
  name: {{ include "group5--nginx-hc.fullname" . }}
  labels:
    {{- include "group5--nginx-hc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "group5--nginx-hc.selectorLabels" . | nindent 4 }}
