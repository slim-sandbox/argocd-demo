apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: szekong-nginx-hc
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: registry-1.docker.io/bitnamicharts
      chart: nginx
      targetRevision: 1.27.4
      helm:
        releaseName: nginx-hc
        valueFiles:
          - $values/manifests/szekong-nginx-hc/values.yaml
    - repoURL: https://github.com/slim-sandbox/argocd-demo.git
      targetRevision: HEAD
      ref: values
  destination:
    namespace: szekong-ns
    server: "https://kubernetes.default.svc"
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
